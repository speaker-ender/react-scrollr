import type { NextPage } from "next";
import FunctionLayout from "../../components/layouts/documentation/function.layout";

const useObserverContextPage: NextPage = () => {
  return (
    <FunctionLayout
      functionName="useObserverContext"
      description={[""]}
      parameters={[]}
    ></FunctionLayout>
  );
};

export default useObserverContextPage;
